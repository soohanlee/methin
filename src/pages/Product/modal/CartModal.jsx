import React from 'react';
import Modalbase from 'components/ModalBase';

const CartModal = ({ isOpen, onOk, onCancel }) => {
  return (
    <Modalbase isOpen={isOpen} onOk={onOk} onCancel={onCancel}>
      asdf
    </Modalbase>
  );
};

export default CartModal;
